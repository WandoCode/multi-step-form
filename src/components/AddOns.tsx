import { FormProps } from '../types/multiStepFormTypes'

function AddOns({ onGoNext, onGoBack, currDatas }: FormProps): JSX.Element {
  return <div className="pickAddons"></div>
}

export default AddOns
