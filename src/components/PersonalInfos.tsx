import { FormProps } from '../types/multiStepFormTypes'

function PersonalInfos({
  onFormSubmit,
  onGoBack,
  currDatas,
}: FormProps): JSX.Element {
  return <div className="personalInofs"></div>
}

export default PersonalInfos
